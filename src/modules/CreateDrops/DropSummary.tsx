import { LinkIcon } from '@chakra-ui/icons';

import { RoundIcon } from '@/common/components/IconBox/RoundIcon';

const data = {
  dropName: 'Star Invasion Invite',
  amountPerLink: '200',
  totalLinks: '25',
  redirectLink: 'starinvader.gg/login',
};

export const DropSummary = () => {
  <RoundIcon icon={<LinkIcon />}></RoundIcon>;
};
